
var tree = require("./utils/tree");

// this will take the restaurant_example.json and break the files into the output folder
tree.breakTreeApart();

// this function will take the inputs inside the output folder and rebuild the restaurant_example.json file
//tree.rebuildTree(["restaurants", "restaurants_address", "restaurants_grades", "restaurants_grades_score"]);


